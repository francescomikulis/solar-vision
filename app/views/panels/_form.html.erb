<%= form_for(@panel) do |f| %>
  <% if @panel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@panel.errors.count, "error") %> prohibited this panel from being saved:</h2>

      <ul>
      <% @panel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="TextInputs">
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name, :class => 'NameText' %>
    </div>
    <div class="field">
      <%= f.label :power %><br>
      <%= f.text_field :power, :class => 'NameText' %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
